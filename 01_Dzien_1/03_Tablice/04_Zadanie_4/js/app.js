

// function printTable(array){
//     console.log(array);
// }
// printTable([1,4,7]);

// function printTable(array) {
//     array.forEach(function (item)
//     {
//
//     console.log(item);
// //    array.forEach(function(item, index, array))
// })
// printTable([1,4,7]);


//## Zadanie 4
//Napisz funkcję ```printTable(array)```, która przyjmuje tylko jeden parametr - tablicę.
// Następnie przy pomocy odpowiedniej metody tablicowej przeiteruj
// (przejdź) po każdym elemencie i wypisz każdy z nich w konsoli.



/*    function printTable(array) {
        array.forEach(function (item) {
            console.log(item);
        })
    }

    printTable([1, 2, 3, 4, 5]);
    printTable(['Jeden', 'Dwa', 'Trzy']);*/

function printTable(array) {
    array.forEach(function (item) {
        console.log(item);
    })
}

printTable([1, 2, 3, 4, 5]);
printTable(['Jeden', 'Dwa', 'Trzy']);

